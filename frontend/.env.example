# IA del agente (Google Gemini) — pega la key y listo (la app la usa sin tocar código)
VITE_GEMINI_API_KEY=
VITE_GEMINI_MODEL=gemini-2.0-flash

# Backend (Supabase) — fase de conexión posterior
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
